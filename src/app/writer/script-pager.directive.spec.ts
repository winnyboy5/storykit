import { ScriptPagerDirective } from './script-pager.directive';

describe('ScriptPagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScriptPagerDirective();
    expect(directive).toBeTruthy();
  });
});
